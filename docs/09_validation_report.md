# Validation report

The deterministic full build verifies seven registered sources and eleven releases, nine Nemotron shards, 1,000,000 unique synthetic adult records, 19 minor cells, 73,249 minor links, 45,170 child-household samples, 16 control cells, 18 categories, and 1,440 archetypes.

National person, minor, household, child-household, establishment, and I56 enterprise reconciliation rows are within 0.5%; observed maximum relative error is floating-point noise. The adult age-band calibration aggregation diagnostic has MAPE 0.0459% and 100% coverage under the ±1,000 published-rounding tolerance. It re-sums the same age-band × sex controls used for fitting and is not an independent out-of-sample holdout. Current generated evidence records `independent_holdout_status: not_available` and explains that no observations were separated from fitting. Nemotron fitted age-band × sex cells have post-calibration MAPE 0%, weight range 42.451–47.144, and Kish ESS ratio 99.91%.

The current database-enabled Python suite records 67 passed with one Starlette deprecation warning; without the database integration environment it records 65 passed / 2 skips. All 46 registered engine validation checks pass: lineage, periods, probability ordering, unit coverage, category bounds, ≥1,200 archetypes, ≥80/category, unique rules, estimate status coverage, and inferred-need disclosure. Rare-output tests verify suppression for derived human units below Base 10, numeric redaction, the explicit official-method-provenance exception, suppression of an exact weighted human Base of 5, business-unit exceptions, the Base=10 boundary, and sanitization of persisted legacy suppressed payloads. This is a bounded engine gate, not certification of the original Goal. Twenty representative acceptance queries execute with interval/status, period, method/formula, release lineage, confidence, and gap/warning expectations; unsupported cases correctly return NULL `not_estimable`. Three TAM/SAM/SOM scenarios execute.

The Phase 1-only `001_core.sql` migration creates 30 tables. The current migration source and disposable initial/full-reapply harness extend through `018` at 90 public base tables and 17 views; a populated verification clone reached the same boundary, while the original populated `market_engine` database remains deliberately untouched at `016`/90/12. Migration `014` adds nine interval/source-lineage checks, `015` adds five numeric-domain checks, `016` adds four finite-number checks plus a policy-projected catalog view, `017` adds Opportunity snapshot invariants, and `018` adds five release-safe projections. The SQL initial-application and full-reapplication harness passed. The populated clone's two legacy human Base-below-10 rows project as suppressed; runtime raw-table access and future rare subtype-row detail remain disclosed residuals.

The final application checks passed: TypeScript, zero-warning lint, 44 files/261 web unit tests, 6 files/44 actual-PostgreSQL integration tests, production build, and 22 Playwright cases with 5 intended skips across desktop/tablet/mobile. The final local warm-cache performance run generated at `2026-08-25T13:50:49.812Z` has status `remediated_with_remaining_risks`: 10 queries, 8 below 3 ms, maximum 24.128 ms, none above 50 ms, no physical reads, and no temporary spills. `WB-PERF-09` improved from 53.005 ms/51,819 buffer hits to 2.362 ms/1,849 hits. Detailed model values are generated in `reports/quality_metrics.json`, `reports/population_reconciliation.md`, `reports/estimation_quality.md`, and test output. Those reports do not provide a genuine holdout, live-provider result, or production certificate; historical Nemotron acquisition provenance and an independent holdout remain unavailable.
